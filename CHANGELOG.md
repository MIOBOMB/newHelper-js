2.1.7 - ??:04:2026

Breaking changes между 2.1 и 2.2:
- Теперь newHelper инициализируется вручную, если вы использовали "_", просто сделайте так:
window._ = Intl.newHelper()
- _.$ полностью удалён из ядра из-за бессмысленного синтаксического сахара, полифиллы можно найти по адресу https://objecthub.xyz/static/nhConfig.js
- _.wins теперь `new Map`, все ваши обращения к объекту придётся переделывать на _.wins.get(), либо же вы можете вытащить движок окон из 2.1 если очень боитесь ломать код. Я тоже потратил 3 часа на перевод _.wins[] в _.wins.get() в object hub'е
_.hotkeys.keys теперь `new Map` вместо объекта, аналогично пункту выше
_.lang.win() переименован в winTitle(), если вы не использовали .win() напрямую проигнорируйте

Добавлено:
- _.pipe/_.pipeAsync - полифилл для pipe оператора, ну слишком он удобный я прям хочу уже
- _.form - читатель и записыватель форм, для автосохранения самое то
- _.tables - генератор таблиц

Изменено:
- теперь _.lazy умеет хранить вложенные пути
- теперь _.link не инициализируется автоматически
- теперь _.err не инициализируется автоматически
- _.drag портирован из окон наружу
- _.wins теперь `new Map`
- _.lang.win() переименован в winTitle()
- _.hotkeys.keys теперь `new Map` вместо объекта
- _.html теперь умеет вставлять внутрь себя dom узлы

Удалено:
- _.$ из-за ненадобности

2.1 - 25:02:2026

Добавлено:
- Глобальный объект "_"
- _.link — базовый роутер с поддержкой команд-состояний
- _.lazy — ленивая загрузка через прокси
- _.lang — реактивная мультиязычность
- _.http — базовый http клиент поверх xhr
- _.$ — DOM хелперы
- _.html — генератор DOM из шаблонных строк
- _.storage — изолирующая обёртка для Storage
- _.err — глобальная обработка error & rejection
- _.hotkeys — движок горячих клавиш
- _.win — широкий движок окон с перетаскиванием, сворачиванием и полноэкранным режимом
